#include "main.h"
/**
 * main - prints the alphabet, in lowercase
 * followed by a new line
 * Always: Return 0
 */
void print_alphabet_x10(void)
{
        char la;

        for (la = 'a'; la <= 'z'; la++)                                         _putchar(la);

        _putchar('\n');
}
